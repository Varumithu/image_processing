#include "LocalThresholdFilter.h"


void LocalThresholdFilter::filter() {

	for (size_t i = 0; i < image.size().height; ++i) {
		for (size_t j = 0; j < image.size().width; ++j) {

		}
	}

}